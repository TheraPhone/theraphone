import 'package:flutter/cupertino.dart';

class TheraPageFast extends StatelessWidget {
  const TheraPageFast({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
